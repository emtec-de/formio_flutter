/// Handle the [key] and [data] of each [Class] that is gonna be called.
class Manager {
  /// retrieve the [key] of the [WidgetParser] class implemented.
  // ignore: missing_return
  String? keyValue() {}

  /// retrieve a [dynamic] data of the [WidgetParser] class implemented.
  dynamic get data {}
}
